Project 2: Flask Puzzle Server

This project implements CRUD routes (GET, POST, PUT, DELETE) for the /users endpoint using Flask.

Form Analysis:
The provided HTML form sends user input via POST with the following obfuscated field names:

doggy → username
zebra42 → password
rocketShip → age
kittycat → email

Extra Credit:
My favorite drink is cappuccino!
